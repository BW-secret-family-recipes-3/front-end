import React from 'react';
import { connect } from "react-redux";

function AddRecipe(props){
    return(
        <div>
            <h2>Add Recipe</h2>
            {/* AddRecipe component goes here*/}
        </div>
    );
};

export default AddRecipe;